import styled from 'react-emotion';

export const Container = styled.div({
  display: 'flex',
  position: 'relative',
  zIndex: 100,
  justifyContent: 'center',
  alignItems: 'center',
  height: '100%',
  height: '100vh',
});
